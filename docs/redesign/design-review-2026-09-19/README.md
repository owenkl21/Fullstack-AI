# Design review and simplification, 19 September 2026

Owen's Claude Design export, kept whole so the frames can be opened later.
Open `Fisherfeed Rework.dc.html` for the overview, then the three canvases.
`frames/` holds every frame on its own: `<id>.html` is the exact spec (inline
styles), `<id>.png` its render, `<id>.note.txt` its annotation.

Owen's instruction on handing it over: build the parts exactly as here; keep
the waves (the app's waterline) as they are, with no straight-line section
breaks; everything else was simplified for a reason, so follow it to the dot.
Where a frame shows a straight edge under the black plate, that is only the
export's art placeholder not rendering: the app keeps its wave.

Frames: 1a to 1h the log; 2a to 2e the feed card; 3a to 3k competitions.

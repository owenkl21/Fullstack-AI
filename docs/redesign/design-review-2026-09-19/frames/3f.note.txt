3f Step 3 · who can enter, and the checks The six checks are named the way they appear on an entry later, so the organiser learns them once. Whether a passing entry counts at once or waits for review is one Segment.